<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.server.mapper.TestMapper">

    <select id="list" resultType="com.example.server.domain.Test">
        select t.id, t.name from `test` t
    </select>

    <select id="list2" resultType="com.example.server.domain.Test">
        select id , name from `test`
    </select>

</mapper>